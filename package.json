{
  "name": "kubeviz",
  "version": "0.0.1",
  "description": "Turn Kubernetes manifests into an interactive map (provisional name)",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "kubeviz": "dist/cli.js" },
  "files": ["dist", "templates"],
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": { "yaml": "^2.5.0" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
