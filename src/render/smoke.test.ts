import { test } from "vitest";
// Rewritten in Task 4 for the React Flow renderer. The previous Cytoscape-based
// assertions no longer apply after the Task 2 engine swap.
test.skip("render smoke — rewritten in Task 4 (React Flow)", () => {});
